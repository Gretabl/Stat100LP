---
title: "Stats 100 Project"
author: "Greta Lane and Rowan Pollard"
format: revealjs
editor_options: 
  chunk_output_type: console
---

```{r}
library(rmarkdown)
library(tidyverse)
library(janitor)
library(lubridate)

```

# Simulate Data
```{r}
simulated_ghr_data <-
  tibble(
    country =
      c("Argentina", "USA", "Belize", "China", "France", "Italy", "Japan", "Kenya", "Germany", "Canada"),
    happiness =
      runif(n = 10, min = 0, max = 10),
    healthy_mortality =
      runif(n = 10, min = 55, max = 85)
  )


simulated_HDI <-
  tibble(
    country =
      c("Argentina", "USA", "Belize", "China", "France", "Italy", "Japan", "Kenya", "Germany", "Canada"),
    income =
      runif(n = 10, min = 20000, max = 100000),
    healthcare =
      rbinom(n = 10, size = 1, prob = .5)
  )
```

#plots for the simulated data
```{r}
ggplot( simulated_HDI) +
  geom_col( aes(x = income, y = healthcare, colour = country)) +
  labs( x = "income", y = "healthcare")


```

#load data 
We imported a cleaned excel datasheet. 

#organize and clean data
```{r}
New_HDI <- read_csv("data/New_HDI.csv", show_col_types = FALSE)
head( New_HDI )

new_hdi <- clean_names(New_HDI) |>
  filter( hdi_rank != ".." ) |>
  mutate( hdi_rank = as.numeric( hdi_rank ) )
  
#hey blud this command isn't working and we can't figure it out
new_hdi |> 
  arrange(hdi_rank, .by_group = FALSE)
  
  
```

#make models for the real data
```{r}
#make look better
ggplot(new_hdi) +
  geom_point( aes( y = gross_national_income_gni_per_capita, x = life_expectancy_at_birth)) 
  
ggplot( new_hdi ) +
  geom_point( aes( y = mean_years_of_schooling, x = expected_years_of_schooling ))
>>>>>>> 4d3eb628a2efe48d28564af4c60330637bef9b8e

```




